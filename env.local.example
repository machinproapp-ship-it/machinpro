# Copy to .env.local and fill in. Do not commit secrets.

# Web Push (VAPID) — optional; push is skipped when keys are missing.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:noreply@machin.pro
